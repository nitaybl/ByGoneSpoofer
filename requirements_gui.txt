# ByGone Spoofer GUI - Requirements
# Install with: pip install -r requirements_gui.txt

# Core dependencies (same as console version)
colorama>=0.4.6
requests>=2.31.0
pywin32>=306

# GUI framework (modern, sleek UI)
customtkinter>=5.2.0

# Optional but recommended for better experience
Pillow>=10.0.0  # For image handling in GUI

# Build tools (for compilation)
pyinstaller>=6.0.0

